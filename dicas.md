###CÓDIGOS GIT

cd

clear

ls

git add \*

git init

git clone

git commit -m

git push origin main/master

git remote -m

git remote add origin

git remote -v = listar repositórioscadastrados

git status
